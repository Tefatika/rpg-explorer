using RPGExplorer.Actions;

namespace RPGExplorer.Rooms
{
    /*
    static class Common
    {
        public static Entity CreateSky()
        {
            var sky = new Entity("Sky", "Above", "Firmament", "Up");
            sky.SetActionResultMessage(ActionType.Examine, "The sky is obfuscated by the clouds.");
            sky.SetActionResultMessage(ActionType.Go, "Yeah.\nYou forgot the noclip cheat code.");
            sky.SetActionResultMessage(ActionType.Hit, "I can't jump that high.");

            return sky;
        }

        public static PhysicalEntity CreateCemeteryGround()
        {
            var ground = new PhysicalEntity("Ground", "Floor", "Down", "Downward", "Terrain", "Below", "Grass", "Field");
            ground.SetActionResultMessage(ActionType.Examine, "The grassy ground is full of puddles; I must be careful not to ruin my boots!");
            return ground;
        }

        public static PhysicalEntity CreateCemeteryPuddle()
        {
            var puddle = new PhysicalEntity("Puddle");
            puddle.SetActionResultMessage(ActionType.Examine, "Yeah. It's water. With a hint of mud. Shouldn't the two of us be doing something else here?");
            puddle.SetActionResultMessage(ActionType.Eat, "Don't.");
            puddle.SetActionResultMessage(ActionType.Take, "I don't have anything on me to put it in.");
            puddle.SetActionResultMessage(ActionType.Go, "*SPLASH*\nDarn! My boots!");
            puddle.SetActionResultMessage(ActionType.Hit, "*SPLASH*\nDarn! My boots!");

            return puddle;
        }

        public static PhysicalEntity CreateGround()
        {
            var ground = new PhysicalEntity("Ground", "Floor", "Down", "Downward", "Terrain", "Below");
            return ground;
        }

        public static PhysicalEntity CreateCeiling()
        {
            var ceiling = new PhysicalEntity("Ceiling", "Above", "Up");
            return ceiling;
        }
    }
    */
}